# can_reader
A Jupiter notebook that show parsing, decoding, visualizing and analyzing of vehicle CAN data.
Included to the project open source samples of an MF4 and DBC files.
For different data, the column names can differ, so make sure you change the column names accordingly, when you download the project and load your data.
